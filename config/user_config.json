// Values in here override those in core_config.json

{
	// Only clients from these IP addresses can connect

	"allowed_addr": [ "127.0.0.1" ],

	// Set 'use_auths' to false to bypass user authentication

	"use_auths": true,

	// Users.

	"users": {
		"myuser": {
			"password":
				"sha1$498e6519$1$9ec4ded71edb6d1b8c2b634df1901d1b18b2b979",
			"auths": [ "view", "setprop", "manage" ]
		},
		"rob": {
			"password": "plainpass",
			"auths": [ "view" ]
		}
		},

	// Set 'useSSL' to false if you don't want to use SSL

	"useSSL": true,

	"blacklist": [
		"svc:/system/system-log:default"
		]
}
